export type EventData = {
    id: number;
    name: string;
    latitude: number;
    longitude: number;
    date: string;
    channelId: string;
};

export type Point = {
    lat: number,
    lng: number
}